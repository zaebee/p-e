# v0.13 — решения по #67 + #81 на подпись редактора

Статус: лист решений, не решение. Заполняет bee.zae. Ни одна опция не
помечена предпочтительной (конвенция `relay-lite-v0.13-decisions.md`).
Основания обязательны: `relay-0940` установил, что стоимость альтернативы —
не основание; `relay-0925`/`relay-0927` — что пустая reason-колонка честнее
выдуманной (deletion-log rule, #53).

Контекст: план `relay-lite-v0.13-plan-67-81.md`, тред `relay-0924`…`relay-0952`
(9 атак: 7 bee.claude + 2 relay-mimo), записка
`relay-lite-v0.13-independent-recommendations.md` (трекается, `d2005af`).

---

## D1. Существует ли `history/` в v0.13?

- A. Да: append-only архив, все опубликованные записи включая errata.
- B. Нет: транспорт без архива (тогда cleanups 1 и 3 из round 1 — отвергнуты,
  фиксируется в deletion log строкой UNEXECUTED-отказ).
- Решение: **A** — принято bee.zae 2026-09-08, записано `relay-0960`.
- Основание: **ДАНО.** Формулировка — relay-grok, `relay-1093`; **принята
  bee.zae 2026-09-11**. Протокол обязан различать «запись никогда не
  публиковалась» и «запись опубликована и не удерживается». Без нормативного
  места для опубликованных записей это различие **не является состоянием
  протокола**: отсутствие байтов неотличимо от небытия. `history/` как
  append-only архив есть поверхность, на которой потеря становится **названным
  состоянием** (`KNOWN_MISSING` или равносильным), а не догадкой читателя.
  Утверждение о **наблюдаемости** потери, не о её предотвращении: `relay-0960`
  уже записал честный предел — архив делает потерю записываемой и удержания не
  гарантирует (на 1049 записях названы и не удерживаются три). Поддерживающая
  оговорка, не второе независимое основание: append-only как **обязанность
  протокола**, а не привычка одного развёртывания, требует места в
  спецификации; наблюдаемое хорошее поведение правилом не является.
  **Основанием НЕ является** цена B против A (исключено `relay-0940`),
  «в репозитории и так так» и «раунд так склонялся» (описания), и «erratum'у
  нужен дом, переживающий цель» — сильная интуиция, принадлежащая **D2**,
  которая своё основание получает отдельно.
  Кто не писал: **bee.claude** — `#53` запрещает основание от стороны,
  оформившей варианты, и `relay-0960` применил это к нему поимённо; вариантов
  A и B он автор. relay-grok не писал ни вариантов, ни `relay-0960`.
- Следствие для D2 (не решение): единственное не-ценовое основание за D2-B —
  «`history/` нет, errata некуда переезжать» — снято. B остаётся доступным и
  теперь требует собственного основания.
- Следствие для лога (не решение): строки 5 и 18 становятся закрываемыми как
  executed, когда v0.13 понесёт каталог. Строка 26 ждёт D2.

## D2. Где живут errata и на что распространяется бан?

- A. Вывод раунда: `errata/` ликвидировать, erratum — обычная запись
  истории; `expired/` для TTL-sweep (стоимость: draft + TTL addendum +
  `src/relay-lite/cns.ts` + тесты).
- B. Ратификация разворота: `errata/` = expired records (смысл v0.12),
  errata — payload shape в §3 без своей секции.
- Scope бана (при любом): какие директории именуются (`in/` / `errata/` /
  `tmp/` имеют lifecycle с move/reap — жалоба round 1 стоит)?
- Решение:
- Основание (НЕ стоимость альтернативы):

## D3. Форма `erratum` (near-consensus треда, нужна формальная приёмка)

Кандидат: `target_id` + `target_digest` + непустой `reason`; БЕЗ
`superseded_by` (не определён в v0.1 — forward pointer); claim-framing;
fixed token для метки (adopt-now, все трое); дисклеймер рядом с нормой.

- Принять / отклонить / изменить:
- Основание:

## D4. Clause-4 v8 (store duty — кандидат, не решение)

Кандидат: STORE, возвращающий запись, возвращает и held correction status
как CLAIM под FIXED TOKEN, marked INCOMPLETE + non-coverage disclaimer.
Consumer-bound форма **не** отозвана как класс: `relay-0953`
отозвал именно это («WITHDRAWN AS A CLASS was too wide») и сузил невозможность
до egress — обязанность на то, что сторона **пишет в стор**, проверяема. Прежняя
строка листа инвертировала эту поправку (`relay-0967` F6, `relay-0975`).

- Принять / отклонить / изменить:
- Основание:
- **Предупреждение о подмене предмета** (`relay-1153`): `relay-1150` подаёт «на
  executive recording» запись, озаглавленную D4, чей предмет — **принятие
  спецификации работы 3** (фрейм доставки и проводной формат утверждения).
  **Предмет D4 — клауза 4 v8, обязанность стора**, и её кандидат есть поправка к
  `relay-0952`, то есть **работа 1, приостановленная `relay-1062`**. Сама
  `relay-1150` адоптации не утверждает («presented for executive recording»,
  «formally invited to record»), и назначенный атакующий пишет то же:
  «'uncontested' is a warning state, not a clean bill», перечисленное им —
  «D4-acceptance preconditions the sheet must still price», а «the closing test
  names executive action only» (`relay-1149`); его подтверждение `relay-1151`
  прямо исключает «executive recording» из покрытого. **Работа 3 закрылась по
  правилу 14 и заслуживает строки — под собственным именем; строки для неё в
  листе нет, потому что при написании листа её не существовало.** Открыть её —
  bee.zae.

## D6. Привязка Settle (открыто атаками, `relay-0984`)

Строка 18 лога — Settle, move в `history/YYYY-MM/` — `ABSENT` **на оси v0.1**
(цитирует строку 96 треда, внутри тела v0.1): раундами время Settle не
решалось, поэтому **обе привязки — новый дизайн**, и ни одна не наследует
готового основания (`relay-0987`). Список не исчерпывающий: живы ещё Settle
отдельным актом депонирующего и ленивый Settle по первой ссылке; Settle по
первому чтению мёртв (чтения невидимы). От привязки зависит статус `expired/`:

- A. **Settle на публикации**: запись входит в `history/` при публикации,
  в `expired/` только копии, свипер не уничтожает записей, правило удержания
  не нужно. Цена — неограниченная очередь **и изменение модели доставки**:
  §2.1 односегментна, а publish-привязка делает каждый акт видимым всякому
  читателю стора. Требует основания как изменение модели, не как выбор каталога.
- B. **Settle на claim** (форма v0.1): незаявленные акты истекают как
  последние экземпляры; возвращается трихотомия — тихая потеря,
  архив-по-недосмотру или схлопывание `expired/` в `history/`.
- Решение:
- Основание:

## D7. Определение «стора» (условие принятия D4)

D4 не принимается, пока «стор» не определён; поведенческий кандидат
(`relay-0974`) умер под атакой `relay-0983` / `relay-0984`. Критерии
приёмки любого определения, названные **до** его написания:

1. **Независимость от исполнения обязанности** — определение не может
   состоять в том, что субъект уже делает то, к чему его обязывают.
2. **Покрытие обоих дизъюнктов §7.3** (строка 338): и вывод дайджеста при
   загрузке, и проверка перед коммитом.
3. **Локальная разрешимость** — сторона может установить, связана ли она.
4. Условие (2) D4 — перепроверку покрытия v8 — исполняет сторона, не
   писавшая ни v8, ни определение; атакует третья. **Назначает — диспетч**:
   исполнитель и атакующий называются в записи-диспетче **до** начала работы,
   и потому само назначение атакуемо; bee.zae может переназначить. Без этого
   «третья сторона» выбирается тем, кому выгоден результат (`relay-0987`).

**Пороги запечатаны `relay-1006` (bee.zcode), и оценивает их не bee.claude.**
Порог A, «держит»: владение октетами, **пережившее промежуток, в котором ни один
обмен с этими октетами не открыт**. CDN-edge снаружи, TTL-кэш внутри, стор после
депозита внутри; это факт о жизненном цикле буфера стороны против её же цикла
обменов, а не самоотчёт о долговечности, который критерий (3) запрещает. Порог B,
крепление §7.3: **членство необходимо и достаточно**, обязанность масштабируется
тем, что удерживается, а держатель дайджеста без октетов — вне класса, и §7.3 для
него **пуст**, а не связывает.

**Развилка «одно слово — две роли»: диагноз подтверждён, рецепт переписан.** Двух
определений лист не берёт — **v8 определения не требует вовсе**, он связывается
**глаголом**: тот, кто вкладывает октеты записи в руки другой стороны, отданные
на чтение **или отправленные доставкой** (§2.1, иначе побег режимом доставки).
Асимметрия, из-за которой глагол умер для §7.3 и живёт для v8: вывод, проверка и
подъём ошибки — поведения соответствия, чьё отсутствие в сервисе **невидимо**, а
выдача байтов **и есть** сервис.

**Назначения по критерию 4, объявлены заранее (`relay-1007`):** перепроверку
покрытия v8 под запечатанным определением делает **relay-grok** — не писал ни v8,
ни определения, ни кандидатов; атаку на перепроверку — **opencode**, автор v8.
Назначение предложено оцениваемой стороной, и это сказано вслух, а не спрятано;
bee.zae может переназначить.

**Встречный кандидат bee.claude — удержание «после обмена, в котором получены» —
отозван** (`relay-1007`): «какой обмен» оставалось на усмотрение субъекта, и
буферизующий прокси оказывался внутри при одном чтении и снаружи при другом. Это
ровно тот класс отказа, который запрещает третий критерий, написанный тем же
автором. Починка совпадает с порогом A, значит оси не конкурируют.

**Перепроверка покрытия v8 сделана (`relay-1008`, relay-grok) — в основном
подтверждает, и открывает четыре вопроса.** Все четыре клетки плитки заселены
настоящими процессами; третьего выхода из глагола для прямой выдачи открытого
текста не нашлось. Что осталось незакрытым и передано автору v8:

1. **Косвенная передача** — pre-signed URL или редирект: октеты попадают в чужие
   руки **вследствие** действия стороны, не проходя через её ответ. Внутри при
   причинном чтении «вкладывает», снаружи при узком («тело этого ответа»).
   Рецепт не фиксирует, какое чтение верно.
2. **Только шифротекст.** Если «запись» — это открытые байты акта, выдача одного
   шифротекста может быть третьим выходом; если «запись» — то, что сторона
   выдаёт как представление записи, связана. Рецепт не фиксирует.
3. **Буква v8 уже рецепта.** Клауза говорит «**возвращая** запись» — путь
   чтения; глагол `relay-1006` покрывает и §2.1-доставку. Значит покрытие
   publish сегодня — **артефакт рецепта, а не клаузы**, и D4 не может принять v8
   как написан и одновременно заявлять более широкий субъект. Согласование
   формулировки — за автором v8 (opencode); диспетчер её не переписывает.
4. **По записи, а не по процессу.** Сторона может быть «оба» для одной записи и
   «только v8» для другой. Порог B это уже подразумевает («масштабируется тем,
   что удерживается»), но лист должен сказать прямо: вопрос «является ли этот
   процесс стором» — **неверно поставленный вопрос**.

И различение, которого в рецепте нет: асимметрия «соответствие невидимо, сервис
виден» держится для **срабатывания**, но не вполне для **исполнения** — клиент
видит отсутствие токена исправлений, если схема ответа его требует, хотя не
видит, искала ли сторона errata. Членство и проверяемость — разные вопросы.

**Атакует перепроверку `opencode`** (автор v8), по назначению из `relay-1007`.

**Ход работ по D7 и трём работам — в `docs/notes/d7-thread.md`.** Он занимал
1667 строк из 1796 в этом листе и хоронил шесть неподписанных решений под
историей седьмого. Текст перенесён целиком, без удалений и правок.

**Состояние на 2026-09-12, коротко:**

- **Работа 1** — поправка к `relay-0952`, чертит relay-grok, атакует opencode.
  **Приостановлена** `relay-1062`: четыре черновика умерли, и ни один от
  собственного дефекта — все в суставе, где поправка связывает субъект,
  недоопределённый D7. Возобновляется, когда определение запечатано и
  пережило атаку.
- **Работа 2** — токен-гейт, чертит bee.zcode, атакует relay-grok.
  **Молчит** с `relay-1044`; шесть находок `relay-1047` без ответа. Разбор
  `relay-1154`: четыре из шести отвечены спецификацией работы 3, одна
  (G5) отвечена и спит до D3a, одной (G2) не хватает одной фразы.
- **Работа 3** — размещение утверждения, чертил gemini, атаковал opencode.
  **Закрыта**: 35 черновиков, `relay-1151` — подтверждение атакующего о нуле
  открытых требований по сорока находкам. Строки в листе у неё нет, потому
  что при написании листа её не существовало; открыть — bee.zae.
  Попытка записать её результат под меткой D4 помечена в D4 как подмена
  предмета (`relay-1153`).
- **Пороги.** Порог A запечатан `relay-1006`, привязан якорями `relay-1023`,
  получил квалификатор авторитета в `relay-1044` после `relay-1038`/`relay-1040`.
  Клауза осмотрительности прошла четыре черновика; последний —
  `relay-1037`, атакован `relay-1040`.
- **Что блокирует извне:** D6 («settled» — ноль вхождений в черновике v0.12,
  `relay-1083`) и D3a (у эрратума нет поля цели, `relay-1088`). Обе
  всплыли снизу, из работы 3, и обе неподписаны.

## D5. Отложенное (именовано, не решается здесь)

- Половина #107 (enum: сократить до определяемого vs shape всем четырём) —
  baseline сменился (см. план), решать с видимой доской.
- Проекции: core clause vs profile (возражение grok приложено).
- `§6` numbering: дыра с примечанием vs перенумерация с записью.
- Erratum от relay-mimo за выдуманную loss-census цитату (`relay-0952`
  запрошен, ожидается).

---

## Подпись

- Редактор:
- Дата:
- Relay-запись с решениями (после подписания — депонировать, id сюда):
