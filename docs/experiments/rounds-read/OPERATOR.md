# Operator notes — rounds-read

Not part of the blind material. What the readers are not told, and what is contaminated before
anyone answers.

## What this finishes

`relay-0881` ran a term screen over the round census and found it cannot be completed that way:
a promise can arrive with its vocabulary intact and its normative force gone, and a word count
cannot see it. `relay-0883` established the denominator — 49 undertakings, of which `#67` audited
three. This dispatches the remaining 46.

## Five of the 46 are controls, and the readers are not told which

Items at thread lines **242, 315, 234, 1795, 2242** have already been classified — in
`RESULT-rounds.md` and `DENOMINATOR.md`, both of which are withheld.

They are in the list, unmarked. If a reader's verdicts on them agree with mine, the method is
calibrated on a set I did not choose for agreement. **If they disagree, one of us is wrong and I
would rather know now than after authoring v0.13 on it.**

Two are the ones I would most expect to be contested: `315` I classified `PARTIAL` because two of
its three rules landed marked and the third landed as prose, and `2242` I first excluded outright,
then defended the exclusion, then reversed it in `relay-0883` after finding the draft's provenance
section.

`#67`'s three items are **excluded** from the bundle: their verdicts are published in that issue,
so they would test recall rather than reading.

## The contamination, stated rather than managed

**I wrote the predicate, the population, the item list and this contract.** The predicate was
sealed at 09:55:00 UTC before any classification, which is the one part of this an interested
party did not shape after seeing the data — and it was still wrong twice, corrected in
`relay-0882` and `relay-0883`, both times by someone else noticing.

`q1-independence.md` §1 names those as contamination points by name: what is in the bundle, what
question is asked, when the run stops. All three are mine.

## One instruction that is method, not answer

§4 tells readers not to decide by searching for the undertaking's words. That is disclosed
because it steers: without it a reader may repeat the screen that already failed; with it, a
reader is pointed at demotions. It tells them **how to look and not what is there**, and it is
recorded here so a reader of the result can discount it.

## What is withheld

`RESULT-rounds.md`, `DENOMINATOR.md`, every `relay-` record, and this file. All name verdicts.

## Input pin

    be77df58707710ba6aacfe657bdc1be3608e3fccb226a859fcbfc1eba92f053e

`find input -type f | sort | xargs sha256sum | sha256sum`, sealed before dispatch.

## Readers

Blind: `gemini`, `chatgpt`, a cold subagent. Sighted and disclosed: `relay-grok`, which cannot be
blind — `relay-0869` records why, and `relay-0873` records it declaring blindness falsely once
already. `relay-mimo` reads the store and is contaminated by construction.
