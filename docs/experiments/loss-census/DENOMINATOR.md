# The denominator, established

`relay-0882` recorded that the round census's population was wrong in both directions and that
the true number was not known. This establishes it, item by item, with every verdict checkable
against two line numbers.

**49 undertakings, one of them arguable. Not 48, and not the 51 candidates the corrected pattern
returns.**

## The test, which is the sealed predicate's, unchanged

> "One **undertaking**: a numbered item inside a response section. Its author states what will
> change in the draft. … An undertaking is not a finding."

Applied to each candidate: **does the item state a change to the draft?** A retrospective states
what happened. The verdicts below are the whole of the classification and each is one reading of
one item.

## Candidates

Two forms, both inside response sections:

| form | count |
|---|---|
| `### N. <title>` headings | 48 |
| plain `N. **<title>:**` under `### Structural Cleanups:` | 3 |
| **candidates** | **51** |

Every other plain-numbered group in the thread was checked and excluded before classifying:
**29 items** under `Conformance Summary`, `Specification Status v0.4`–`v0.11`, `Summary of v0.7`
and the archival summary are recaps of `### N.` items already counted; **two groups** are v0.1's
own body, outside response sections entirely.

## Excluded from the population — 2

| # | line | item | why |
|---|---|---|---|
| 36 | 1582 | `Current Invariant Baseline (v0.8 Working Draft)` | Lists five things already undertaken — JCS, I-JSON, `to[]`, tri-state, POSIX semantics. A recap under a response heading, promising nothing new. |
| 51 | 2253 | `Transition to Implementation` | *"The review phase for Issue #5 is formally closed."* A statement about the process, not the draft. v0.12 records no review closure. |

## Three I had wrong, corrected here

`relay-0881` and `relay-0882` treated status-statements as obviously not undertakings. Checking
v0.12 shows otherwise.

| # | line | item | corrected verdict |
|---|---|---|---|
| 34 | 1564 | `Retraction of Premature "Logical Completeness"` | **UNDERTAKING, and it landed** — v0.12 line 3: *"**Status:** Working draft. Not verified. Not adopted by this project."* |
| 48 | 2164 | `Re-affirmation of Status: Working Draft (v0.12-draft)` | **UNDERTAKING, and it landed** — same line, and the title *"relay-lite — Working Draft v0.12"* |
| 46 | 2134 | `Epistemic Status of the 6-State Causal Partition` | **UNDERTAKING** — it says how the partition is to be *recorded*: "not as an independently verified universal law, but as a formal hypothesis … subject to falsification". `hypothesis` and `falsifi` each occur once in v0.12; whether that discharges it is for the full read |

## And one I excluded too firmly — 2242

`relay-0881` classified item 50 (`Archival Summary of the 14-Round Review Record`) as
`EXCLUDED`, *"a retrospective, not an undertaking to change the draft"*, and `relay-0882`
defended that.

v0.12 carries a **`## Provenance and standing`** section at line 347 and a provenance paragraph
at lines 4-8 naming the review's shape — *"revised across sixteen review rounds between one
proposer and one reviewer … Contest language across all fifteen replies: zero."*

**So the draft does discharge the function 2242 named**, in different content: 2242 promised a
summary of four hardened subsystems; v0.12 records provenance and standing instead.

Whether that is the same undertaking landing altered, or a different thing occupying its place,
is a reading — and the predicate says such cases are named, not resolved silently. **Counted as
an undertaking, flagged as arguable.** The earlier exclusion was firmer than its evidence.

## Result

| | |
|---|---|
| candidates | 51 |
| excluded — recap or process, not draft changes | 2 |
| **undertakings** | **49** |
| of which arguable (`2242`) | 1 |
| audited by `#67` | 3 — of which **1 landed** |
| read in full so far | 5 |
| **owed a full read** | **41** |

## What this changes about #109

The issue asked for "43" and then for the denominator to be established first. It is **41**, and
the three items corrected above are already classified — `1564` and `2164` `LANDED`, `2134` open.

## What it does not settle

Nothing about whether the 41 landed. The denominator is a count of promises, not of kept ones,
and the screening blind spot `relay-0881` recorded still stands: a term count cannot see an
undertaking that arrived with its marking dropped.
