# Result — the `UNEXECUTED` sweep

Predicates and `extract.py` sealed at `d9d9b1c` before any item was classified, and applied by a
reader that had not seen this session. Both pins re-verified by that reader; `ITEMS.txt`
regenerated byte-identical from the committed script.

## The sheet's own defect, found by the run

**Both controls name items the sealed filter excludes.** `extract.py` matches
`^### (\d+)\. `. Thread line 249 reads `### Structural Cleanups:` — **no number** — and its three
resolutions are ordinary list items at 250, 251 and 252. Verified: `grep -cE '^(250|251|252)\|'`
over `ITEMS.txt` returns **0**.

`C1` named thread 250 (eliminate `.relay/errata/`). `C2` named thread 251 (envelope id
cross-check). **Both are outside the 48.** The reader classified them correctly and both
predictions came out true — and **neither tested the filter, which is the only thing a control is
for.**

**So the census built to sweep this axis cannot see the row that motivated it.** `relay-0922`
added the `UNEXECUTED` shape because of thread 250, and this population excludes thread 250. The
same block hides thread 252, *"Directory Mutation Scoping"*, which is also unexecuted —
`.relay/active/` is deletion-log row 16 and the `history/` ban is rows 4 and 5.

## Counts

| code | n | |
|---|---|---|
| `EXECUTED` | 43 | 89.6% |
| `UNEXECUTED` | **2** | |
| `NOT-A-RESOLUTION` | 2 | |
| `MULTI` | 1 | |
| `SUPERSEDED` | **0** | |

Primary denominator **47** (48 less the `MULTI`); `UNEXECUTED` rate **2 of 47**.

### The two

**Thread 242 — the dual-order model.** Round 1 resolved to name arrival order, scope it to worker
draining, and deny it causal force. v0.12 has **nothing**: `arrival`, `queue`, `drain`, `worker`
and `lexicograph` occur zero times. Already **deletion-log row 21**, on the v0.1 axis as `ABSENT`.
Searched 2,016 following lines for a withdrawal: none.

**Thread 2253 — the transition to implementation.** Commit the spec at `spec/relay-lite.md`,
write the implementation in `src/relay/`, fuzz the classifier. **Not in the deletion log at all** —
which is what `P2` asked for. It is the last block in the thread; nothing could have withdrawn it.

### `SUPERSEDED` is empty, and that is a result

Every withdrawal across sixteen rounds happened **below heading level** — the tie-break key
(`digest` → `id`), `logical_seq`, `to` leaving and re-entering the act, the digest basis moving to
wire octets. In each case the heading's own object survived into v0.12, so no whole heading was
ever reversed.

## Where the reader says the sheet is wrong, and it is right

**Thread 2253's object is not the specification's content.** It is three actions on a repository,
and a document cannot contain its own commit path — so the code was forced and reads stronger than
it is. **Two of the three bullets were carried out at other paths:** `src/relay-lite/` holds ten
modules and nine test files (verified). What is literally unexecuted is the **path** —
`spec/relay-lite.md` was never created and `src/relay/` was declined on purpose, which v0.12 line
13 says outright.

**Scope `UNEXECUTED` to resolutions about the specification's content and the count is 1.** Then
`P1` fails at its floor and `P2` fails, because the survivor is already row 21. **Three of four
predictions turn on one contested item.**

## Predictions

| # | predicted | result |
|---|---|---|
| **C1** | thread 250 codes `UNEXECUTED` | ✅ **and off-population** |
| **C2** | thread 251 codes `EXECUTED` | ✅ **and off-population** |
| **P1** | `UNEXECUTED` between 2 and 8 | ✅ **2, at the floor**, one of them contested |
| **P2** | at least one not already a log row | ✅ 1 of 2 |
| **P3** | `NOT-A-RESOLUTION` < `EXECUTED` | ✅ 2 < 43 |
| **P4** | thread 2208 onward gives zero `UNEXECUTED` | ❌ thread 2253 is in that window |

## The counting rule is still not sealed tight

I sealed *"one heading is one unit"* and the `MULTI` escape, and **did not seal the test for
"independent"**. The reader supplied one and states it so it can be overturned:

> A heading is `MULTI` iff the commitments its block **newly** introduces do not all receive the
> same code.

And says plainly that under a different test threads 242 and 2253 both become `MULTI` and
**`UNEXECUTED` collapses to zero.**

**Fifth count predicate, same failure.** `relay-0903`, `relay-0908`, `relay-0910`, `relay-0914`,
and now this — and this time I sealed the rule that was supposed to prevent it and left the next
one open. One count predicate in the series has survived: the one applied by a reader who did not
write the sheet.

## Also reported, and not resolved here

**Demotion has no code in this vocabulary.** The deletion log applies `DEMOTED` to review
resolutions too — rows 19, 20, 22, 23, 24, 25 cite thread lines inside the rounds. Did v0.12
"reflect" a resolution that said `MUST` and landed as unmarked prose? The reader coded those
`EXECUTED` to avoid double-counting a measurement the log already holds, and says the choice is
its own: coding them `UNEXECUTED` would put the count at **7**.

Finding: `relay-0966`.
